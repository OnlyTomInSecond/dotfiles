# =============================================================================
#  .zsh_extra — Aliases, keybindings, env vars, helper functions (优化版)
# =============================================================================

# ===============================================================================
# 1. Terminal keybindings — 精简版
# ===============================================================================
# 仅绑定最常用的键，减少 terminfo 查询和 bindkey 调用
typeset -g -A key

key[Home]="${terminfo[khome]}"
key[End]="${terminfo[kend]}"
key[Delete]="${terminfo[kdch1]}"
key[Up]="${terminfo[kcuu1]}"
key[Down]="${terminfo[kcud1]}"

[[ -n "${key[Home]}"   ]] && bindkey -- "${key[Home]}"   beginning-of-line
[[ -n "${key[End]}"    ]] && bindkey -- "${key[End]}"    end-of-line
[[ -n "${key[Delete]}" ]] && bindkey -- "${key[Delete]}" delete-char

# Up/Down: 按已输入前缀搜索历史（替代默认的行-by-行）
autoload -Uz up-line-or-beginning-search down-line-or-beginning-search
zle -N up-line-or-beginning-search
zle -N down-line-or-beginning-search
[[ -n "${key[Up]}"   ]] && bindkey -- "${key[Up]}"   up-line-or-beginning-search
[[ -n "${key[Down]}" ]] && bindkey -- "${key[Down]}" down-line-or-beginning-search

# Ctrl+Left/Right: 按单词跳跃
bindkey "^[[1;5C" forward-word
bindkey "^[[1;5D" backward-word

# 终端应用模式 — 必须启用，否则 Home/End 等键的 terminfo 映射失效
if (( ${+terminfo[smkx]} && ${+terminfo[rmkx]} )); then
    autoload -Uz add-zle-hook-widget
    function zle_application_mode_start { echoti smkx }
    function zle_application_mode_stop { echoti rmkx }
    add-zle-hook-widget -Uz zle-line-init zle_application_mode_start
    add-zle-hook-widget -Uz zle-line-finish zle_application_mode_stop
fi

# ===============================================================================
# 2. Aliases
# ===============================================================================
# 着色
alias ls='ls --color=auto'
alias grep='grep --color=auto --binary-files=without-match'
alias fgrep='grep -F --color=auto -i -n'
alias egrep='grep -E --color=auto -i -n'
alias diff='diff --color=auto --strip-trailing-cr'

# 快捷
alias ll='ls -ahl'
alias la='ls -A'
alias l='ls -CF'

# 安全
alias mv='mv -i'
alias cp='cp -i'
alias rm='rm -i'

# 导航
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

# 杂项
alias mkdir='mkdir -pv'
alias df='df -h'
alias free='free -h'

# ===============================================================================
# 3. Word navigation — 自定义单词边界
# ===============================================================================
WORDCHARS='*?_-.[]~=&;!#$%^(){}<>'

# ===============================================================================
# 4. Autoloads — 按需加载，不在启动时注册
# ===============================================================================
# 只在首次使用时 autoload（已在 .zshrc 中处理的部分此处省略）
autoload -Uz zmv            # zmv '(*).txt' '$1.md'
autoload -Uz zcalc          # zcalc

# ===============================================================================
# 5. zsh-autosuggestions — 只用 history 策略（completion 策略显著拖慢补全）
# ===============================================================================
ZSH_AUTOSUGGEST_STRATEGY=(history)
ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE='fg=8'

# ===============================================================================
# 6. Ctrl+R: fuzzy history search
# ===============================================================================
bindkey '^R' history-incremental-pattern-search-backward
bindkey -r '^S'

# ===============================================================================
# 7. Environment variables
# ===============================================================================
export GCC_COLORS='error=01;31:warning=01;35:note=01;36:caret=01;32:locus=01:quote=01'

export USE_CCACHE=1
export CCACHE_EXEC=/usr/bin/ccache
export CCACHE_DIR="/run/media/whh/Develop/.ccache/"

export RUSTUP_UPDATE_ROOT='https://mirrors.tuna.tsinghua.edu.cn/rustup/rustup'
export RUSTUP_DIST_SERVER='https://mirrors.tuna.tsinghua.edu.cn/rustup'

# ===============================================================================
# 8. Helper functions — 惰性定义（使用时才注册到环境）
# ===============================================================================

# captive_login: 打开 captive portal 登录页
captive_login() {
    emulate -L zsh
    local gw
    gw=$(ip --oneline route get 1.1.1.1 2>/dev/null | awk '{print $3}')
    if [[ -n "$gw" ]]; then
        xdg-open "http://${gw}" &>/dev/null &
        echo "Captive portal opened for gateway: ${gw}"
    else
        echo "Could not determine default gateway" >&2
        return 1
    fi
}

# update_grub: 更新 EFI GRUB（谨慎使用）
update_grub() {
    emulate -L zsh
    local efi_partition efi_dev mount_opts
    efi_partition=$(lsblk -fr 2>/dev/null | grep -i vfat | head -1 | cut -d ' ' -f 1)
    if [[ -z "$efi_partition" ]]; then
        echo "Error: No VFAT (EFI) partition found" >&2
        return 1
    fi
    efi_dev="/dev/${efi_partition}"
    mount_opts="rw,noatime,fmask=0022,dmask=0022,codepage=437,iocharset=ascii,shortname=mixed,utf8,errors=remount-ro"

    echo "=> Mounting ${efi_dev} to /efi"
    sudo mount -o "${mount_opts}" "${efi_dev}" /efi || return 1

    echo "=> Installing GRUB"
    sudo grub-install --target=x86_64-efi --efi-directory=/efi --bootloader-id=Arch --boot-directory=/efi || {
        sudo umount -R /efi 2>/dev/null
        return 1
    }

    echo "=> Generating grub.cfg"
    sudo grub-mkconfig -o /efi/grub/grub.cfg || {
        sudo umount -R /efi 2>/dev/null
        return 1
    }

    sudo sync
    echo "=> Unmounting /efi"
    sudo umount -R /efi
    echo "Done."
}
